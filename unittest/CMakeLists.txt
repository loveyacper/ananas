PROJECT(UNITTEST)

INCLUDE(../CMakeCommon)

AUX_SOURCE_DIRECTORY(. UNITTEST_SRC)

LINK_DIRECTORIES(../../leveldb)
LINK_DIRECTORIES(../QedisLib)
INCLUDE_DIRECTORIES(../QedisLib)
INCLUDE_DIRECTORIES(../QBase)

ADD_EXECUTABLE(qunittest ${UNITTEST_SRC})
SET(EXECUTABLE_OUTPUT_PATH  ../../bin)
TARGET_LINK_LIBRARIES(qunittest  qedislib; leveldb)
ADD_DEPENDENCIES(qunittest qedislib)
