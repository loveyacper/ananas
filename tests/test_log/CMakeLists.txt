#PROJECT(TESTCLIENT)

INCLUDE(${PROJECT_SOURCE_DIR}/CMakeCommon)

AUX_SOURCE_DIRECTORY(. LOG_SRC)

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR})

ADD_EXECUTABLE(tlog ${LOG_SRC})
SET(EXECUTABLE_OUTPUT_PATH  ${PROJECT_SOURCE_DIR}/bin)

TARGET_LINK_LIBRARIES(tlog ananas)
ADD_DEPENDENCIES(tlog ananas)
