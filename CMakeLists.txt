CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(ANANAS_TEST)

SUBDIRS(net)

IF(${CMAKE_SYSTEM_NAME} MATCHES "Linux")
SUBDIRS(coroutine)
ENDIF()

SUBDIRS(tests)
SUBDIRS(unittest)
SUBDIRS(examples)
SUBDIRS(protobuf_rpc)
